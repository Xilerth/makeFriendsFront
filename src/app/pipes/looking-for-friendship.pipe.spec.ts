import { LookingForFriendshipPipe } from './looking-for-friendship.pipe';

describe('LookingForFriendshipPipe', () => {
  it('create an instance', () => {
    const pipe = new LookingForFriendshipPipe();
    expect(pipe).toBeTruthy();
  });
});
